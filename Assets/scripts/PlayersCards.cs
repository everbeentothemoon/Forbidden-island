using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class PlayersCards : MonoBehaviour
{
    public class playerHand
    {
        [SerializeField]
        private List<ForbiddenIslandCards> cards;
        private float limitOfCards = 5f;

        public PlayerHand()
        {
            cards = new List<TrainCards>();
        }

        public void AddCard (TrainCards trainCards)
        {
            cards.Add(trainCards);

            //this if statement needs to be checked my brother in christ
            if (cards.Count > limitOfCards)
            {
                cards.Remove(trainCards);
            }
        }

        public void RemoveCard(TrainCards trainCards) 
        { 
            cards.Remove(trainCards); 
        }
    }
}
*/