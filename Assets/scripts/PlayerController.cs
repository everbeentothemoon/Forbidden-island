using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class PlayerController : PlayersCards
{
    [SerializeField]
    private playerHand playerhand;
    [SerializeField]
    private ForbiddenIslandCards FIcardManager;
    // Start is called before the first frame update
    private void Start()
    {
        for (int i = 0; i < 2; i++)
        {
            playerHand.AddCard(FIcardManager.GetNewCard());
            playerHand.AddCard(newCard);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/